const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({ datasources: { db: { url: 'postgresql://postgres:Inioluwa%402025@127.0.0.1:5432/jesaworldsch' } } }); async function main() { try { const count = await prisma.subject.count(); console.log('Subject count:', count); if(count>0) console.log(await prisma.subject.findFirst()); } catch(e) { console.error(e); } } main();
